{
  "name": "staynear-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "install": "cd backend && npm install"
  }
}
